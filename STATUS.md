# Lock-In / War Room Remediation Status

> Governing document: `MASTERPROMPT.md` (read-only). Branch: `refactor/strategic-judgment-os`. Last verified: 2026-08-22. Phase 0 (security) and Phase 1 (correctness/performance) are **COMPLETE** in source and local tests; Phase 2 (`/catchup` + recovery scoring) is **COMPLETE**. No production deployment, production migration, production D1 backup, Cloudflare change, secret change, credential rotation, Cron deployment, or repository-visibility change is claimed here.

## Current Gate

- PHASE-0 [DONE — SOURCE/LOCAL VERIFIED] Book 5 security and data integrity is complete: 5.1–5.7 implemented and tested, every Book 5.8 acceptance item COVERED (5.8.1 now exhaustive). Full local suite green.
- PHASE-1 [DONE — SOURCE/LOCAL VERIFIED] Book 6 correctness and performance: canonical timezone with explicit DST tests, `/api/state` bounded at 19 statements flat across history, polling replaced by an ETag `/api/version` probe with focus/action/boundary refresh, morphdom-style in-place render preserving focus and half-typed text, XSS escaping proven, multi-write paths batched, single enforcement pass.
- PHASE-2 [DONE — SOURCE/LOCAL VERIFIED] `/catchup` re-entry protocol (Book 8.6) and Minimum Viable Recovery scoring (Book 8.2): migration `0009`, `/api/catchup`, `/api/recovery`, streak survival on `mvr_held`, `needsCatchup` signal, and the frontend re-entry door.
- PHASE-3 [DONE — SOURCE/LOCAL VERIFIED] Alternative-explanation gate (Book 13.2): migration `0010` adds `heat` + `alternative_explanation` to captures, an append-only counted `alternative_explanations` ledger, and DB triggers that refuse a non-calm capture with no alternative. `/api/intel` enforces the brake (clean 400) and records the ledger; `/api/stats` surfaces the total and the "none plausible" paranoia-tell count; the capture form and STATS view carry it. Evidence: `test/alt-explanation-gate.test.ts` (11) + `test/alt-gate-wiring.test.ts` (4).
- PHASE-4 [DONE — SOURCE/LOCAL VERIFIED] Chapter cursor + Continuity Brief (Books 16/R9 + 14): migration `0011` adds `chapter_cursor` (a DB value, seeded to Chapter 2 — Anaphora on first read, never a code constant). `GET/POST /api/cursor` read and set it; the Commander's File briefing now carries the cursor line; `GET /api/continuity-brief` emits the plain-text session brief (active front, cursor, cycle day, progress, review cadence, phase position, last debrief, next move) that survives external compaction; COUNCIL has a copy action. Evidence: `test/cursor-continuity.test.ts` (11).
- PHASE-5 [DONE except operator-scoped alarms — SOURCE/LOCAL VERIFIED] Book 7 server structure is complete. `src/index.tsx` **3790 → 262 lines (−93%)**, decomposed into ~32 modules, each extraction verified against the full gate: leaf utilities (security-headers, time, scoring, validation, env, schemas, crypto, renderer), repository (repositories), services (streak, cursor, commanders-file, ai, enforcement, state, curriculum), support (clock, auth, request-support), the `agent-auth` middleware module, and TWELVE route modules under `routes/` (agent-v1, hermes, intel-library, tongue, learn, recovery, cursor, day, economy, auth, agent-credentials, calendar). index.tsx is now purely the composition root: app + global/CSRF/auth middleware + the `/api/*` and agent-auth guards + route registration + the manifest/sw/shell render routes. **Dedup fix:** the agent-auth extraction had left the entire pre-extraction copy behind (232 lines) alongside the import of the same names, so esbuild silently shadowed one binding and the guard was running the dead local copy; removing it makes the guard resolve through `./agent-auth`, proven by the agent-route denial and scope tests still passing. Also removed 34 dead imports and the orphaned `./schemas` import block. **Frontend delegation DONE:** all 73 inline `onclick/onchange/onkeydown` sites across `app.js`..`app7.js` were converted to a single delegated dispatcher (`data-act` + JSON `data-args`, per-module `registerActions`), eliminating the app's own need for `script-src 'unsafe-inline'`; complex inline expressions (mode toggles, `this`/`event` handlers, the reader `<select>`) became named handlers; behaviour unchanged (handlers still close over the same `let`-globals). A happy-dom net (`frontend-boot`/`frontend-interactions`, 7 tests) boots the real frontend and drives auth-submit + nav + block-log through the dispatcher; `frontend-delegation.dom.test.ts` (2) statically forbids any inline handler and proves every `data-act` maps to a registered handler. **Citation note:** the `src/index.tsx:NNNN` line references in the B3.* and B5.* entries below predate this refactor and point into the former monolith; the current location of each route/helper is the correspondingly-named `src/routes/*.ts` / `src/*.ts` module. Book 7 status: server structure, frontend delegation, the frontend ES-module/Vite rewrite, table unification (all three kinds + SR + exams) and SM-2→FSRS are all **DONE** — see the dedicated entries below. Only the alarms item (Web Push via Cron) remains, and its VAPID secrets + Cron Worker deployment are permanently operator-scoped, so the repository can build and test it but never activate it. Then Books 8.1, 9, 10, 11, 12, 13, 15 and the PWA/accessibility pass.
- TABLE-UNIFICATION-SLICE-1 [DONE — SOURCE/LOCAL VERIFIED] `migrations/0012_unify_captures.sql` (additive + reversible) creates `captures` (kind-discriminated union of intel_entries + maxims + responses), `review_items` (unifies response_srs; FSRS-ready columns left NULL), and `exams` (unifies tongue_exams), then backfills each 1:1 with `(legacy_table, legacy_id)` provenance. It does NOT drop/alter/delete any legacy table — those remain authoritative and every route still uses them, so runtime behaviour is unchanged and rollback is dropping the three shadows. `test/migration-0012.test.ts` (7) proves count preservation per kind, field-value + owner mapping, legacy tables byte-unchanged, and idempotent re-run. `OPERATIONS.md` §5.8 records apply/verify/rollback. Next slices: dual-write the shadows on every write path, then switch reads feature-by-feature, then retire the legacy tables only after the switch is proven.
- TABLE-UNIFICATION-MAXIMS [DONE — SOURCE/LOCAL VERIFIED] `migrations/0013_maxims_cutover.sql` cuts maxims over to `captures`. D1 enforces FKs, so `flashcards` (the sole home of maxim SR state) is recreated with its `maxim_id` FK now targeting `captures(id)`, rows copied with `maxim_id` remapped via `captures.legacy_id`; the full pre-migration table is retained as `flashcards_pre0013_backup` and the `maxims` table is frozen (never written) as a second backup. `learn.ts` (GET/POST /api/maxims, my-words, GET /api/cards/due), `curriculum.ensureCards`, and the agent export now read/write maxims through `captures`; `claimUnownedData` extended to `captures`/`review_items`/`exams` so a rollback-era unowned row can't be orphaned by the backfill. Evidence: `test/maxims-cutover.test.ts` (7 — backup parity, zero flashcard loss, no orphans, new-FK enforcement, routes read/write captures, card queue intact) + the adapted `legal-state-transitions` flashcard test. `OPERATIONS.md` §5.9 records apply/verify/rollback. Responses (tongue SR) and intel (alt-gate triggers) cutovers follow the same pattern next.
- TABLE-UNIFICATION-INTEL [DONE — SOURCE/LOCAL VERIFIED] `migrations/0014_intel_cutover.sql` cuts intel over to `captures`. `intel_entries` has no incoming FKs, so no table recreation — it is frozen (never written) and kept with its 0010 triggers as backup; the migration only re-creates the Law-23 alt-explanation brake ON `captures` as two `kind='intel'`-scoped triggers (`trg_captures_alt_gate_insert/update`). intel-library (list/create/verdict), hermes analyze, agent read/write + export, commander's-file briefing, and the stats count now read/write intel through `captures`; the app-level `altGate` still returns a clean 400 and the triggers are the schema backstop. Evidence: `test/intel-cutover.test.ts` (4 — writes to captures not the frozen table, reads back, verdict finality, API brake 400, DB trigger backstop) + adapted intel fixtures in audit-fixes/audit-idempotency/legal-state-transitions/model-security. `OPERATIONS.md` §5.10 records apply/verify/rollback. Remaining unification: responses (tongue SR — response_srs/tongue_reviews FKs, coordinated with the FSRS runtime switch).
- TABLE-UNIFICATION-RESPONSES [DONE — SOURCE/LOCAL VERIFIED] `migrations/0015_responses_cutover.sql` completes the unification: response content → `captures(kind='response')`, SR state → `review_items(kind='response')`, and the tongue exam log → `exams(kind='tongue')`. Since 0012 backfilled `review_items.item_id` with the legacy response id, 0015 remaps it onto the new `captures.id` (collision-proof two-phase) and recreates `tongue_reviews` WITHOUT its `responses` FK (D1 enforces FKs; new reviews log a capture id), backing it up first. `responses`/`response_srs`/`tongue_exams` are frozen as backups. tongue routes + `state`/`enforcement`/`commanders-file` now read/write `captures`+`review_items`+`exams`; SM-2 scheduling preserved on `review_items`. Evidence: `test/responses-cutover.test.ts` (4 — SR remap no-orphans + parity, tongue_reviews backup + FK dropped, capture/exam writes land in unified tables while legacy frozen), the adapted `test/tongue-review.test.ts` net (drill queue + scheduling through review_items), and the adapted legal-state tongue fixture. `OPERATIONS.md` §5.11 records apply/verify/rollback. **Table unification COMPLETE** (all three content kinds + SR + exams unified; every legacy table frozen, never deleted). Next: the SM-2→FSRS runtime switch on `review_items`.
- FSRS-FOUNDATION [DONE — SOURCE/LOCAL VERIFIED] `src/fsrs.ts` is a pure FSRS-4.5 scheduler (retrievability decay, interval-for-retention, next Stability/Difficulty per grade) plus a documented one-way `sm2ToFsrs()` migration mapping; grades 0..3 map to FSRS ratings 1..4. `test/fsrs.test.ts` (8) verifies scheduler properties (weight-independent). `test/tongue-review.test.ts` (3) is a store-agnostic behavioural net over the live Tongue review flow (due-queue, success widens spacing, lapse reschedules sooner, not-due→409) that guards the switch.
- FSRS-RUNTIME-SWITCH [DONE — SOURCE/LOCAL VERIFIED] The Tongue review handler now schedules with FSRS on `review_items`: it reads the stored Stability/Difficulty (or seeds from the SM-2 columns as a fallback), computes elapsed days from `last_review` (via new `time.daysBetween`), applies `fsrsReview`, and records `stability`/`difficulty`/`last_review` + the FSRS interval; the SM-2 counters (reps/lapses/total/correct) are retained for the mastery ladder + stats. `migrations/0016_sm2_to_fsrs.sql` backfills FSRS state for existing cards via the stated SQL mapping (additive; reversible). Evidence: `test/fsrs-wiring.test.ts` (2 — 0016 leaves no card without in-range FSRS state; a review records the FSRS state and schedules forward) + the tongue-review net now driving FSRS. `OPERATIONS.md` §5.12 records apply/verify/rollback. The Farnsworth ladder (1/3/7/16/35) remains reserved for the Book 11 rhetoric track per the spec.
- FRONTEND-RESTRUCTURE [DONE — SOURCE/LOCAL VERIFIED] The nine-`<script>`-tags-of-globals frontend is now an ES-module tree under `public/static/app/` bundled by Vite to one `bundle.js`: `core/` (store.js holding all ~30 shared mutable globals, sanitize.js, events.js = the data-act delegation registry, shell.js, fx.js, morph.js) and `features/` (campaign+router, mind, debrief, library, council, tongue), entry `main.js`, config `vite.client.config.ts`, wired into `npm run build`. The shell loads a single script tag. The conversion was mechanical, driven by a masker validated against grep ground truth on all nine files and preserving `${...}` expressions (real code a blind rename would have corrupted); cross-module references became explicit imports from the computed dependency graph. Two gates earned their keep: rollup fails the build on any missing import (the transpile-only worker build never did), and the DOM net caught an implicit global (`renderExtra`, illegal once modules made the code strict) plus a delegation-registry TDZ — which is why `events.js`/`sanitize.js` are dependency-free core modules. The nine old static files are deleted; every raw-source wiring test and the delegation guard now scan the module tree; the morph test asserts the module export and catchup-wiring asserts the delegated-action contract (`window.*` handler globals are gone by design). Harness hardened for determinism: per-boot listener/timer/rAF cleanup, a canvas-2D stub (happy-dom returns null, so FX frames threw as unhandled rejections), and `waitFor` condition polling instead of fixed ticks — three consecutive clean runs. Book 7 items already satisfied: book JSON was already lazy-fetched (never precached) and `scripts/preview-db.cjs` is already out of the root. **Known flakiness (pre-existing, untouched):** the two 60-second-window rate-limit cases in `test/agent-credentials.test.ts` can fail under heavy parallel load; they pass 9/9 in isolation.
- ALARMS-WEB-PUSH [DONE to the scope boundary — SOURCE/LOCAL VERIFIED] `migrations/0017_push_notifications.sql` adds `push_subscriptions`, `notification_preferences` and the append-only `push_deliveries` ledger, whose `UNIQUE(user_id,kind,ref,occurs_on)` makes the minute-by-minute job idempotent. `src/push.ts` signs VAPID (RFC 8292) JWTs with WebCrypto ES256 and sends **payload-less** pushes — no journal text passes through a third-party push service and there is no hand-rolled encryption in the delivery path — and fails closed like the model boundary (absent config ⇒ `503 PUSH SERVICE OFFLINE`). `src/routes/push.ts` adds the owner routes (key, subscribe, unsubscribe, preferences, read-only `/api/next-alarm`) plus `POST /internal/jobs/alarms`, secret-guarded like the enforcement job, deriving block/debrief/review alarms from the server clock with quiet hours and isolating an unreachable device so it cannot abort the run. The service worker gained a `push` handler; the alarms panel arms real push and states the platform limit honestly (works closed on Android/desktop, iPhone only as an installed PWA on iOS 16.4+, `.ics` always rings); the in-page war horn is explicitly cosmetic with durable `localStorage` fired-state. Evidence: `test/alarms-push.test.ts` (11) including a REAL ES256 sign-then-verify, plus the new routes added to the exhaustive denial and GET-read-only gates. **Operator boundary:** VAPID secrets and the Cron Worker are permanently operator-only, so push cannot be activated from the repository. `OPERATOR_HANDOFF.md` is the single ordered file covering every operator action — preflight, the D1 backup gate, applying `0012`–`0017` with non-secret verification SQL and rollbacks, VAPID key generation (the documented command was executed and verified: 65-byte P-256 point whose x/y match the private JWK, 64-byte ES256 signature that verifies), secret names, the complete Cron Worker source with variables/triggers/Access service-token path, deploy notes for the two build artifacts, and the honest platform limits. `OPERATIONS.md` §5.13 documents the migration.
- PHASE-6 [DONE — SOURCE/LOCAL VERIFIED] UX simplification and the ratchet (Book 8.1, 8.3, 8.4, 8.5, 8.7 and Book 9's information architecture).
  - **THE RATCHET (8.1):** `migrations/0018_ratchet.sql` adds `schedule_blocks.ratchet_tier` ('mandatory'|'deck'), `ratchet_state`, and append-only `ratchet_events`; the mandatory set is seeded from the commander's OWN prior declarations (CORE `is_mvd` nominations, else non-negotiables, capped at three) and left EMPTY rather than guessed if he declared neither. `src/ratchet.ts` holds the rules as pure functions (ANCHOR_TARGET 3, HOLD_DAYS 7, DEMOTE_AFTER_MISSES 3). Consequence now covers the mandatory set ONLY — `dayAdherence` scores it (one change inside the scorer, so all eight call sites agree) and both enforcement paths penalise it; deck blocks stay visible, loggable and rewarded but can never cost him. Promotion is earned server-side (free below three anchors, then one per clean seven-day hold, never twice a day); demotion by choice is always allowed; a mandatory block missing three scheduled days running is demoted automatically at ZERO point cost. `/api/ratchet` + a `/api/state` summary derived from blocks already loaded (query budget untouched). Transition rule documented: while no anchor exists the prior rules stand, which is why all 258 pre-existing tests passed unmodified. Evidence: `test/ratchet.test.ts` (11).
  - **TEN STATUSES + MISS DIAGNOSIS (8.3/8.4):** `src/block-status.ts` defines the ten states and their scoring meaning, keeping pending/done/skipped as synonyms so no historical row changes meaning; `completed_late` lands, `rescheduled`/`displaced_by_priority` leave the denominator (no moral weight). The same-day window close still closes the window but now writes `unreported` and files an 'attention' prompt worth ZERO points (8.3: never auto-cancelled; a prompt, not a penalty). `migrations/0019` adds the append-only `block_miss_causes` ledger (one diagnosis per block per day); `GET /api/miss-causes` publishes the eleven-cause taxonomy and `POST /api/blocks/:id/cause` records one, returns the single correction that would prevent the next miss, repairs a forgotten log as `completed_late` for free, marks a genuine displacement/emergency with no moral weight, applies the ordinary −5 only for a genuine miss, and opens an investigation (not points) on repeated avoidance. Setting a status on an unreported block is refused until the cause exists. Evidence: `test/miss-diagnosis.test.ts` (10).
  - **LEDGER LIMITS + RANK CUT (8.5):** `src/scoring-limits.ts` states the two bounds once — a hard daily penalty cap (30) and a ledger floor (0) — and `addFlag` applies both to every penalty; the flag is always recorded in full, only the cost is bounded, and the ledger line says which bound bit. Load reduction moved to the THIRD consecutive miss as 8.5 states (the second draws the cause prompt); because 8.1's demotion answers the same third miss, the demotion is decided before and applied after the load-reduction pass so neither remedy swallows the other. Every miss check now goes through one `didNotLand()` predicate — this caught a real bug where a `completed` block still read as a miss. The rank ladder is CUT (FX.rank removed, header shows the points it re-rendered, STATS shows the ledger + adherence), with the condition for its return documented. Evidence: `test/scoring-limits.test.ts` (6).
  - **LANGUAGE + TONE (8.7):** all five struck-out phrases are gone from the shipped interface (a test scans every server message and client module so they cannot creep back) and the calm replacements are in use. `src/tone.ts` states the severity hierarchy exactly — neutral, information, attention, warning, critical — maps the engine's legacy words onto it ('serious'→warning, 'warn'→attention) and reserves red for `critical` alone; the client has a matching `severityClass()`. The tone setting is real and persisted: `GET/POST /api/tone` with neutral|firm|military|compassionate, defaulting to firm, reported in `/api/state`, refusing an invented register. Military is cold, never contemptuous (asserted). Hermes is untouched — a test proves `src/ai.ts` never imports the tone module (Book 15 governs it). Evidence: `test/tone-language.test.ts` (9).
  - **INFORMATION ARCHITECTURE (Book 9):** the bottom bar is now TODAY, LEARN, PRACTICE, REVIEW, MORE, each with a segmented control whose faces are the tabs they replaced (12 faces total), so nothing became unreachable — one level in instead of nine targets across; badges moved with their surfaces. `src/intel-domains.ts` collapses the seventeen stored domain values to six (people, network, intimacy, money, tactics, wisdom) NON-DESTRUCTIVELY: nothing in the database is rewritten, every row reports the `domain_group` it now belongs to, a filter matches either the new or the legacy value, an unrecognised value lands in wisdom rather than vanishing, and `GET /api/intel/domains` publishes the six. Evidence: `test/information-architecture.test.ts` (7) plus the DOM navigation net now clicking all five tabs AND every one of their twelve faces.
  - **Book 4 compliance:** six new entries in the in-app scoring changelog record every behavioural scoring change above, so the rules of his own game never change silently. `OPERATIONS.md` §5.14–§5.15 and `OPERATOR_HANDOFF.md` cover migrations 0018–0019.
- PHASE-7 [DONE — SOURCE/LOCAL VERIFIED] The learning engine (Book 10, all ten sub-books).
  - **10.1 MEASURED READING:** `src/reading.ts` computes the verdict from dwell + traversal + pace (450 wpm ceiling, 45s floor, 90% traversal, 120s idle gap, all stated once so the changelog can cite them). `migrations/0020` adds `reading_sessions` and an APPEND-ONLY `reading_events` traversal record — which is also 10.1's section-level position record. `POST /api/reading/open|progress|close` accumulate dwell SERVER-SIDE; there is deliberately no endpoint through which a client can declare a chapter read, and a test asserts it. The curriculum gate replaced the button: `step:'reading'` is granted only when a measured session for that unit passed, and the refusal explains what is being measured. Slow reading is always honest; an abandoned tab earns nothing.
  - **10.5/10.6 PROVENANCE:** `sources`/`source_editions`/`source_sections`/`section_variants` record title, author, translator, edition, year, URL, licensing, portion, completeness, editorial notes and checksum. `translation_status` is CHECK-constrained so a translation cannot be called "official" unless it is; partial works are labelled explicitly; sections are anchored at paragraph level with the original term (始計) travelling with the passage; and where editions disagree the rival rendering is stored so the disagreement is displayed rather than silently resolved.
  - **10.2 MASTERY LADDER + RUBRIC:** `src/mastery.ts` holds the six levels with their declared required evidence, the nine dimensions with 10.2's own score meanings, and the admission rules. A level is DERIVED from evidence (the ladder stops at the first gap, so integrated evidence alone cannot make him integrated). `admitEvidence` refuses an explanation outside 50–150 words, retrieval with the source open, a transfer with no populated reference, and integrated status without a 2.5 mean and no zero dimension — and it never reads `self_score`, proven in both directions (a perfect self-score cannot admit weak evidence; a harsh one cannot block good evidence). `migrations/0021` adds the APPEND-ONLY `mastery_evidence` trail and a derived `mastery` cache no client writes.
  - **10.2 ADVERSARIAL GRADING (cheap half, local):** `src/cloze.ts` generates DETERMINISTIC cloze deletions from the stored source text (same passage → same test, so it cannot be rerolled until easy), scores them against the passage, and diffs free recall into recovered/missed/invented. `GET /api/cloze/:anchor` never returns the answers.
  - **10.3 CALIBRATION:** `src/calibration.ts` computes calibration error and Brier terms, buckets reliability by confidence band, and NAMES the pattern in 10.3's own shape ("You rate yourself 4 of 5 and score 2 of 3 on reversal questions"), including the underconfident case and the not-enough-data case. `GET /api/calibration/knowledge` reports the knowledge Brier beside the decision Brier. Nothing subtracts a point, and a test asserts no penalty language.
  - **10.4 R0:** a unit cannot be completed until a same-session, source-closed retrieval attempt exists for it; an open-book answer is recorded but explicitly does not satisfy R0.
  - **10.6 CONCEPT MODEL:** the Five Factors seeded with translation RANGE and the specific distortion each attracts (道 ≠ a simplistic "why"; 天 ≠ luck; 地 ≠ geography alone; 法 defends the supply line — sleep, cash, isolation, calendar). 將 is the commander and the commander is himself: its five virtues (智 信 仁 勇 嚴) are components scored on the LOWEST, with the response naming the weakest virtue and stating that the mean would have hidden it. A guard STRIKES any framing that redirects 將 outward into an inventory of other people's emotional fault lines. The Chapter II oversimplification is corrected and recorded as struck.
  - **10.7 MACHIAVELLI FRAMED:** `principle_frames` carries the ruler-and-state context, civilian analogy, where the analogy BREAKS, the long-term cost and the legal/ethical boundary for every Prince principle, with `do_not_universalise` set and each break condition naming romance/friendship/family.
  - **10.8 GREENE AS HYPOTHESES:** UNREAD, defensive-recognition only, all ten required fields, with `examinable` CHECK-constrained to 0 so the SCHEMA refuses to let one become examinable or seed a syllabus phase.
  - **10.9 IMMUNE TABLE:** fourteen principles seeded from what he has actually read, each carrying naive_reading, master_reading, detection_tells, inversion_trap, less_obvious_application, stop_test and the cost of staying naive; the shaping function cannot emit a master reading without its naive twin.
  - **10.10 PRINCIPLE GRAPH:** all thirty-one named nodes plus ethos/logos/pathos with strong-use and corrupt-use columns, edges to nodes and to paragraph-level anchors, and CONTRADICTION edges whose notes state what selects one principle over its reversal — each rendered with the real question. Cross-book convergence is asserted (more than one source reaches Information).
  - Evidence: `test/measured-reading.test.ts` (13), `test/mastery-calibration.test.ts` (17), `test/principle-graph.test.ts` (11). Two existing unit-step tests were given the honest new preconditions (a measured reading session, an R0 attempt) rather than weakening the gates. Book 4 compliance: four new in-app scoring-changelog entries. `OPERATIONS.md` §5.16–§5.18 and `OPERATOR_HANDOFF.md` cover migrations 0020–0023.
- AUDIT-2026-08-21 [DONE] Cleaning the composition root surfaced defects the vitest+build gate could never catch (esbuild is transpile-only; no test drove the branches). Fixed: five route-module handler branches referenced a helper/schema the module never imported, each a `ReferenceError` that `onError` turned into a 500 on a real path — `hermes.ts` `POST /api/intel/:id/analyze` (`parseEmptyBody`, 500 on **every** call), `day.ts` `POST /api/tick` with an invalid IANA zone (`RequestValidationError`, 500 not 400), `intel-library.ts` `POST /api/library/:bookId/chapter/:idx` unknown-book and out-of-range-chapter (`RequestValidationError`, 500 not 400), and `tongue.ts` `GET /api/tongue?category=…`/`?q=…` (`responseCategorySchema`, `z`, 500 not 200/400). Also widened `altGate(alt)` to `string|null|undefined` to match `optionalText`. TDD regression guard: `test/route-import-integrity.test.ts` (7) drives all paths RED→GREEN. Installed `@types/node` and registered it in tsconfig; **`npx tsc --noEmit` is now 0 errors** (it was silently red on these modules and the DOM tests all along) and joins the gate.
- AUDIT-2026-08-20 [DONE] Full re-read of the MASTERPROMPT against completed work found and fixed six items: (1) **write-on-GET** — `GET /api/cursor` and `/api/continuity-brief` lazily `INSERT`ed a cursor row, a Book 5.3 violation; reads are now read-only (`readChapterCursor` returns the documented default; the row is persisted only by `POST /api/cursor`), and the new GET routes were added to `test/get-read-only.test.ts` which had used a hardcoded list; (2) **future-dated scoring** — browser `POST /api/intel` wrote a raw client `log_date`, so +15 could land on a future day; now clamped never-future via `safeDate` (back-dating preserved); (3) **weak enforcement-idempotency test** replaced with `test/enforcement-idempotency.test.ts` that supplies the job secret and proves single-award end-to-end; (4) **missing in-app scoring changelog** (Book 4) added as `GET /api/changelog` + STATS render; (5) an inaccurate request-id code comment corrected; (6) `OPERATIONS.md` §5.4–5.7 added for migrations 0008–0011. Verified clean: journal/model-output XSS escaping, no auth token in localStorage, no shame language introduced, all eight Book 5.7 idempotency classes wrapped. Forward-looking (not current defects): true offset/cursor pagination (reads are LIMIT-bounded today), extending the alt-explanation gate to decisions/patterns when those phases land, escaping book notes if that surface is ever rendered.
- TESTS [DONE] 342 server tests across 42 files (workers pool) plus 25 DOM tests across 6 files (happy-dom project) = 367 green. `npx tsc --noEmit` is clean (0 errors) and is now part of the gate. Vite build emits two artifacts: the client bundle `public/static/bundle.js` (~151 kB) and `dist/_worker.js` (~225 kB).
- OPERATOR-BOUNDARY [DONE] Production D1 backup/application, Cloudflare Access, Cloudflare secrets, production credential rotation, Cron deployment, repository visibility, and every deployment are permanently operator-controlled. Repository work is limited to code, local schema-copy tests, documentation, commits, and branch pushes.
- ASSUMPTION-2026-08-15 [IN PROGRESS] Smallest defensible ownership migration: create one durable owner from the existing installation during migration, preserve every record, and require independent route authorization; production application remains gated by `OPERATIONS.md`.

## Book 3 — Verified System Inventory

### Routes

- B3.R1 [DONE] Auth — `GET /api/auth/status` at `src/index.tsx:510`; `POST /api/auth/setup` at `src/index.tsx:520`; `POST /api/auth/login` at `src/index.tsx:535`; `POST /api/auth/logout` at `src/index.tsx:563`.
- B3.R2 [DONE] State/enforcement — `GET /api/state` at `src/index.tsx:1224`; `POST /api/tick` at `src/index.tsx:1231`; protected `POST /internal/jobs/enforcement` at `src/index.tsx:1261`.
- B3.R3 [DONE] Blocks/appeals/load — `POST /api/blocks/:id/log` at `src/index.tsx:1278`; `POST /api/appeals` at `src/index.tsx:1329`; `GET /api/appeals` at `src/index.tsx:1384`; `POST /api/load-reductions/:id/answer` at `src/index.tsx:1395`.
- B3.R4 [DONE] Predictions — `POST /api/predictions` at `src/index.tsx:1413`; `GET /api/predictions` at `src/index.tsx:1426`; `POST /api/predictions/:id/resolve` at `src/index.tsx:1433`; `GET /api/predictions/calibration` at `src/index.tsx:1448`.
- B3.R5 [DONE] Debriefs — `POST /api/debrief` at `src/index.tsx:1481`; `GET /api/debriefs` at `src/index.tsx:1522`.
- B3.R6 [DONE] Campaign — `GET /api/campaign` at `src/index.tsx:1563`; `POST /api/units/:id/step` at `src/index.tsx:1581`.
- B3.R7 [DONE] Maxims/cards — `GET /api/maxims` at `src/index.tsx:1673`; `POST /api/maxims` at `src/index.tsx:1679`; `POST /api/maxims/:id/my-words` at `src/index.tsx:1692`; `GET /api/cards/due` at `src/index.tsx:1713`; `POST /api/cards/:maximId/review` at `src/index.tsx:1724`.
- B3.R8 [DONE] Flags — `POST /api/flags/:id/ack` at `src/index.tsx:1758`; `GET /api/flags/history` at `src/index.tsx:1767`.
- B3.R9 [DONE] Tongue — `POST /api/tongue` at `src/index.tsx:1787`; `GET /api/tongue` at `src/index.tsx:1810`; `PUT /api/tongue/:id` at `src/index.tsx:1831`; `DELETE /api/tongue/:id` at `src/index.tsx:1843`; `GET /api/tongue/due` at `src/index.tsx:1855`; `POST /api/tongue/:id/review` at `src/index.tsx:1876`; `GET /api/tongue/exam` at `src/index.tsx:1934`; `POST /api/tongue/exam/submit` at `src/index.tsx:1944`; `GET /api/tongue/stats` at `src/index.tsx:1968`.
- B3.R10 [DONE] Laws/rewards/stats — `GET /api/laws` at `src/index.tsx:2010`; `POST /api/laws/:id/check` at `src/index.tsx:2020`; `GET /api/rewards` at `src/index.tsx:2041`; `POST /api/rewards/:id/redeem` at `src/index.tsx:2045`; `GET /api/stats` at `src/index.tsx:2075`.
- B3.R11 [DONE] Intel — `GET /api/intel` at `src/index.tsx:2178`; `POST /api/intel` at `src/index.tsx:2194`; `POST /api/intel/:id/verdict` at `src/index.tsx:2214`; `POST /api/intel/:id/analyze` at `src/index.tsx:2787`.
- B3.R12 [DONE] Library/calendar — `GET /api/library` at `src/index.tsx:2246`; `POST /api/library/:bookId/chapter/:idx` at `src/index.tsx:2260`; `GET /calendar.ics` at `src/index.tsx:2316`.
- B3.R13 [DONE] Hermes — `POST /api/hermes` at `src/index.tsx:2672`; `GET /api/hermes/history` at `src/index.tsx:2741`; `POST /api/hermes/council` at `src/index.tsx:2749`.
- B3.R14 [DONE] Agent lifecycle — retired `GET /api/agent/token` at `src/index.tsx:2838` and retired `POST /api/agent/token/rotate` at `src/index.tsx:2841`; owner-session-only credential issuance at `src/index.tsx:2848`, safe metadata listing at `src/index.tsx:2879`, and per-credential revocation at `src/index.tsx:2904`.
- B3.R15 [DONE] Versioned agent operations — POST-only briefing, pending blocks, debrief reads, intel reads/writes, debrief writes, block logs, Hermes messages, and export are at `src/index.tsx:2924-3096`. Read-like operations accept an empty JSON body because credential usage/rate/audit accounting writes D1; unsupported GET/HEAD methods are rejected before agent authentication so they remain zero-write.
- B3.R16 [DONE] Shell/PWA — `GET /manifest.json` at `src/index.tsx:3099`; `GET /sw.js` at `src/index.tsx:3105`; `GET /` at `src/index.tsx:3130`.
- B3.R17 [DONE] Counts — current source has 67 explicit routes: 26 GET, 39 POST, one PUT, and one DELETE; no explicit HEAD handler exists. Nine `/api/agent/v1/*` operations are explicit POST handlers, and credential lifecycle adds three owner-session routes.

### Data

- B3.D1 [DONE] `migrations/0001_initial_schema.sql` defines `schedule_blocks`, `block_logs`, `debriefs`, `phases`, `units`, `unit_progress`, `maxims`, `flashcards`, `card_reviews`, `honesty_flags`, `points_ledger`, `rewards`, `reward_redemptions`, `laws`, `law_checks`, and `settings` with the columns recorded in that migration plus additive changes in `migrations/0004_reforge.sql`.
- B3.D2 [DONE] `migrations/0002_intel_books_alarms.sql` defines `intel_entries`, `book_progress`, and `hermes_messages`.
- B3.D3 [DONE] `migrations/0003_tongue.sql` defines `responses`, `response_srs`, `tongue_reviews`, and `tongue_exams`.
- B3.D4 [DONE] `migrations/0004_reforge.sql` defines `day_summary`, `predictions`, `appeals`, and `load_reductions`, and adds weighting/MVD and flag-reference fields.
- B3.D5 [DONE] `migrations/0005_sessions_and_ownership.sql` adds durable `users` and hashed-token `sessions`, adds and backfills `user_id` on all 23 personal tables, and preserves the legacy verifier and all seeded rows in `test/migration-0005.test.ts`.
- B3.D6 [DONE — SOURCE/FOCUSED LOCAL TESTED] `migrations/0006_agent_credentials.sql:11-67` additively creates `agent_credentials` and `agent_credential_events`, their ownership/lifecycle indexes, and erases the legacy plaintext `settings.agent_token` value while retaining its row. `test/migration-0006.test.ts:17-92` proves personal-row preservation, required columns, the full declared scope set, and lifecycle-event insertion.
- B3.D7 [DONE — SOURCE/FOCUSED LOCAL TESTED] `migrations/0007_model_security.sql:11-114` additively creates owner-bound `model_requests`, metadata-only append-only `model_audit_events`, owner/time indexes, atomic per-owner request/token budget enforcement, and audit update/delete rejection. `test/migration-0007.test.ts:7-99` proves personal-row preservation, required columns, the pinned model constraint, and the D1 rate trigger. Total current schema is 33 tables across seven migrations.

### Frontend, integrations, and dependencies

- B3.F1 [DONE] `public/static/app.js` owns setup/login, API calls, NOW/DAY, blocks, appeals, load reduction, drafts, navigation, and refresh via `POST /api/tick`.
- B3.F2 [DONE] `public/static/app2.js` owns campaign/unit progression; `public/static/app3.js` owns maxims/cards; `public/static/app4.js` owns debriefs/rewards/predictions/stats; `public/static/app5.js` owns alarms/books/calendar; `public/static/app6.js` owns Hermes/intel/bridge; `public/static/app7.js` owns Tongue; `public/static/fx.js` owns effects.
- B3.F3 [DONE — SOURCE/FOCUSED LOCAL TESTED] Bridge credential UI holds a newly issued raw credential only in memory at `public/static/app6.js:28`, requests safe credential metadata at `public/static/app6.js:35`, excludes `export:read` from defaults at `public/static/app6.js:29-32`, issues per-device credentials at `public/static/app6.js:64-66`, and revokes them individually at `public/static/app6.js:71`. Closing the modal clears the one-time raw value.
- B3.F4 [DONE — SOURCE/FOCUSED LOCAL TESTED] `public/static/hermes_bridge.py` reads `WARROOM_URL`, an owner-only credential file selected by `WARROOM_TOKEN_FILE`, and optional Telegram settings; requires an absolute HTTPS base URL; authenticates only with `X-Agent-Token`; POSTs empty JSON to read-like v1 routes; uses `(5, 30)` timeouts and default TLS verification; distinguishes 401, 403, 429, and 5xx; and refuses export without `--authorize-full-export`. Static safety assertions are in `test/hermes-bridge.test.ts:5-43`.
- B3.F5 [DONE — SOURCE/LOCAL VERIFIED] Owner-session-only model routes call only the exact HTTPS `OPENAI_BASE_URL` authorized by explicit `OPENAI_ALLOWED_BASE_URLS`, use pinned `gpt-5-mini-2025-08-07`, bound Chat Completions output with `max_completion_tokens`, fence untrusted D1 context in policy order, validate strict model-authored JSON, reserve per-owner D1 budgets before provider contact, record metadata-only audit events, and persist only validated answers through owner-filtered server writes. Focused evidence is in `test/model-security.test.ts`.
- B3.F6 [DONE] The service worker is generated by `src/index.tsx:3105`; it lazy cache-first stores only `/static/books/*`, does not intercept private APIs, has no shell precache/offline write queue/old-cache cleanup, and uses `warroom-v1`.
- B3.F7 [DONE] `package.json` has Hono and Zod as runtime dependencies and Vite, Wrangler, Vitest, the Cloudflare Workers pool, and Workers types as development dependencies. `vite.config.ts`, `vitest.config.ts`, `wrangler.jsonc`, and `workers/enforcement-cron/wrangler.jsonc` are the verified runtime/test configurations.
- B3.F8 [DONE] Credential/configuration names—not values—are `DB`, `OPENAI_API_KEY`, `OPENAI_BASE_URL`, `ENFORCEMENT_JOB_SECRET`, `ALLOWED_ORIGINS`, `CF_ACCESS_CLIENT_ID`, `CF_ACCESS_CLIENT_SECRET`, `WARROOM_URL`, `WARROOM_TOKEN_FILE`, `TG_BOT_TOKEN`, `TG_CHAT_ID`, and `WATCH_INTERVAL`. Values were not copied.

### Confirmed discrepancies and risks

- B3.X1 [DONE] Governing document repository identity differs from actual configured repository `trevor93/Lock-in-2`.
- B3.X2 [DONE] `README.md` still describes production as pending and lists eight tabs. Its schema inventory is reconciled to the current verified count of 33 tables across seven migrations (`README.md:66`); production state remains evidence-sensitive and repository work claims no deployment.
- B3.X3 [DONE] Inspected production is older than source: production `/api/auth/status` returned 404 while source defines it.
- B3.X4 [DONE] Governing provisional service-worker inventory says book data is precached; source lazy-caches each book only after a successful request.
- B3.X5 [DONE] `src/index.tsx` remains a multi-responsibility server monolith. The fixed Book 7 refactor remains deferred until Phase 0 and the deliberately earlier product corrections pass.
- B3.X6 [DONE] Dependency installation reported audit advisories and blocked install scripts. These are observations, not confirmed exploitable defects; dependency remediation may not displace P0 work.

## Book 5 — Phase 0 Security and Data Integrity

### 5.1 Perimeter

- B5.1.1 [BLOCKED — OPERATOR] Production agent credential exposure is remediated in source but remains a production risk until operator deployment and rotation under `OPERATIONS.md`.
- B5.1.2 [BLOCKED — OPERATOR] Full-host Cloudflare Access remains operator-controlled under `OPERATIONS.md`.
- B5.1.3 [BLOCKED — OPERATOR] Repository privacy remains operator-controlled. `MASTERPROMPT.md` is ignored in `.gitignore` until the operator confirms signed-out repository denial; `OPERATIONS.md` tells the operator to notify the owner before the ignore is removed and the exact file is committed.
- B5.1.4 [BLOCKED — DESTRUCTIVE] Git-history stripping is destructive and cannot proceed without backup, stated consequence, and explicit approval.

### 5.2 Sessions and ownership

- B5.2 [DONE — SOURCE/LOCAL TESTED] `migrations/0005_sessions_and_ownership.sql` creates the durable owner and hashed-token session store, preserves the legacy verifier, and additively backfills `user_id` across all 23 personal tables without deleting or rebuilding records. Browser sessions use random opaque tokens, SHA-256 hashes at rest, expiry, `HttpOnly; Secure; SameSite=Lax` host-only cookies, logout revocation, login rotation, and fixation denial in `src/index.tsx:419-507` and `src/index.tsx:510-568`. Personal browser, calendar, Hermes/model-context, internal-enforcement, and current agent-bridge queries enforce the credential/session owner. `test/migration-0005.test.ts:6` proves exact per-table row-count preservation and ownership on a populated pre-`0005` schema copy; `test/session-ownership.test.ts:96` covers cookie attributes, hashed storage, production-format expiry, revocation, rotation, rollback-era row claiming, ownership columns, cross-user reads/updates/appends, calendar, Hermes context, agent export, and load reductions. The full 26-test suite, production build, TypeScript no-emit check, and diff-integrity check passed on 2026-08-15. This is explicitly a durable single-owner boundary, not self-service multi-tenancy; Book 5.5 still owns the agent-credential redesign.

### 5.3 Read safety and enforcement

- B5.3.1 [DONE — SOURCE] Private path classification is `src/index.tsx:29`; private-response middleware begins at `src/index.tsx:64`.
- B5.3.2 [DONE — SOURCE] Shared enforcement is `src/index.tsx:1249`, browser invocation remains `POST /api/tick` at `src/index.tsx:1231`, and protected internal invocation is `src/index.tsx:1261`.
- B5.3.3 [DONE — TESTED] `test/get-read-only.test.ts:113` proves repeated `GET`/`HEAD /api/state` executes zero mutating SQL; `test/get-read-only.test.ts:131` covers every explicit private GET/HEAD route and missing-token paths.
- B5.3.4 [DONE — TESTED] `test/security-boundary.test.ts:244` and `test/security-boundary.test.ts:263` prove the internal route is POST-only, authenticated, server-clocked, and consequence-idempotent.
- B5.3.5 [DONE — SOURCE/TEST] Scheduled caller implementation is `workers/enforcement-cron/src/index.ts:8`; scheduled handler is `workers/enforcement-cron/src/index.ts:26`; Access-header coverage is `workers/enforcement-cron/src/index.test.ts:30`. Deployment is operator-controlled.

### 5.4 CORS, headers, validation, and CSRF

- B5.4.1 [DONE — SOURCE/LOCAL TESTED] The global response boundary at `src/index.tsx:29-134` defaults browser CORS to same-origin, accepts only explicitly configured origins, handles allowlisted credentialed preflight without wildcard CORS, keeps the separately authenticated Termux bridge available to non-browser requests, applies `Cache-Control: no-store` to private success/error responses, sets CSP with `frame-ancestors 'none'`, `X-Content-Type-Options: nosniff`, `Referrer-Policy: no-referrer`, and a restrictive `Permissions-Policy`, and caps API/internal payloads at 64 KiB. `test/security-boundary.test.ts:58-241` covers the browser origin boundary, private no-store behavior, calendar protection, required headers, issued/restored CSRF proof, valid/invalid/missing CSRF proof, and oversize rejection.
- B5.4.2 [DONE — SOURCE/LOCAL TESTED] Browser session setup/login/status issue the derived SHA-256 CSRF proof from the session helpers at `src/index.tsx:431` and `src/index.tsx:478-486`, returned by the three entry points at `src/index.tsx:510-561`; cookie-authenticated API mutations require it at `src/index.tsx:98-109`. The frontend stores the proof only in memory and attaches it to same-origin unsafe requests at `public/static/app.js:6-16`, `public/static/app.js:67-74`, and `public/static/app.js:405-415`. Agent requests with `X-Agent-Token` and the independently authenticated internal job do not inherit the browser-cookie CSRF contract.
- B5.4.3 [DONE — SOURCE/LOCAL TESTED] Zod is a direct runtime dependency. Controlled malformed-JSON handling, strict empty-body parsing, and parameter/query parsing are at `src/index.tsx:136-372`. All explicit mutation handlers parse strict schemas or an explicit empty body; route IDs, book IDs/chapter ranges, dates, times, enums, bounded strings, booleans, integers, arrays, and numeric ranges are validated. Parsed schema output is the only assignment input, so unknown fields and mass assignment are rejected. `test/request-validation.test.ts:82-270` covers 14 malformed/unknown/mass-assignment/identifier/range/date/query/time/empty-body/valid-request cases, including rejection of identifiers outside JavaScript's safe-integer range.
- B5.4.4 [DONE — SOURCE/LOCAL TESTED] Legal workflow transitions are enforced for ordinary units, active/failed/passed exams, book reading/completion, prediction resolution, intel verdicts, due flashcard/tongue reviews, archived tongue responses, and browser/agent attempts to rewrite auto-missed blocks. `test/legal-state-transitions.test.ts:108-510` contains 9 focused tests and proves terminal rewards are not duplicated.
- B5.4.5 [DONE — VERIFIED] On 2026-08-15, focused validation passed 14/14, focused legal transitions passed 9/9, focused browser-boundary coverage passed 10/10, the complete suite passed 54/54 across 7 files, `npx tsc --noEmit` passed, and the Vite production build passed with 108 modules and `dist/_worker.js` at 175.28 kB. `git diff --check` reported no whitespace errors and LF-to-CRLF warnings for `OPERATIONS.md`, `STATUS.md`, `test/security-boundary.test.ts`, and `test/session-ownership.test.ts`; a value-suppressing secret-pattern scan across source, public assets, workers, and root configuration found zero high-confidence credential signatures. The independent read-only reviewer could not complete because its provider returned HTTP 429, so no reviewer approval is claimed. Book 5.4 introduces no schema change or migration; rollback is application-code rollback under `OPERATIONS.md` Section 6 while retaining the additive Book 5.2 schema.

### 5.5 Agent tokens as scoped credentials

- B5.5 [DONE — SOURCE/LOCAL VERIFIED] `migrations/0006_agent_credentials.sql:11-67` adds owner-scoped hashed credentials, device labels, explicit JSON scopes, expiry, revocation, last-used/coarse request metadata, atomic fixed-window rate state, and append-only credential lifecycle events; it erases the legacy plaintext setting value without deleting its row. Source defines all nine scopes and excludes `export:read` by default at `src/index.tsx:351-365`, normalizes IPv4 `/24` and IPv6 `/48` metadata at `src/index.tsx:583-624`, atomically consumes a 60-request fixed window at `src/index.tsx:650-684`, separates hashed agent authentication from browser sessions at `src/index.tsx:686-728`, and enforces method/version/scope at `src/index.tsx:730-828`. Owner-session-only issue/list/revoke routes are at `src/index.tsx:2848-2921`; raw tokens are returned only by issuance POST, while GET listing selects neither token nor hash. Nine v1 operations are POST-only at `src/index.tsx:2924-3096`, preserving the absolute GET/HEAD zero-write rule despite usage/rate/audit accounting. `test/agent-credentials.test.ts:126-439` covers one-time disclosure, hash-only storage, safe listing, scope/method/version/header-only enforcement, revocation, expiry, default export denial, explicit export permission, lifecycle/coarse metadata, compressed IPv6, concurrent/sequential rate limiting, and the 64 KiB boundary. `test/migration-0006.test.ts:17-92`, `test/session-ownership.test.ts:439-514`, `test/get-read-only.test.ts:112-176`, `test/legal-state-transitions.test.ts:325-364`, and `test/hermes-bridge.test.ts:5-43` add migration, ownership, zero-write, legal-transition, and bridge safety evidence. `public/static/hermes_bridge.py` fails safely with HTTPS/TLS verification, bounded timeouts, status distinctions, configurable base URL, credential-file loading instead of process-environment disclosure, and explicit full-export authorization. `OPERATIONS.md:296-386` records migration/verification/rollback evidence and `OPERATIONS.md:442-515` records operator-only issuance/revocation. Final local verification on 2026-08-15 passed 43/43 focused tests and 70/70 full-suite tests. TypeScript no-emit, Python compile, and the Vite production build passed; the build transformed 108 modules and emitted `dist/_worker.js` at 181.72 kB. `git diff --check` reported no whitespace errors and only LF-to-CRLF working-copy warnings. A value-suppressing scan of 31 source/public/worker/root-configuration files found zero private-key, GitHub-token, OpenAI-key, AWS-key, or agent-token signatures. Both independent spec and code-quality reviewers failed before returning findings because their provider returned HTTP 429; no reviewer approval is claimed. No production migration, deployment, credential issuance/rotation, or other operator action is claimed.

### 5.6 Model security, prompt injection, and cost

- B5.6 [DONE — SOURCE/LOCAL VERIFIED] Model security, prompt-injection defense, and cost control are implemented and locally verified.
  - Owner-only: the three model routes (`POST /api/hermes` at `src/index.tsx:2672`, `POST /api/hermes/council` at `src/index.tsx:2749`, `POST /api/intel/:id/analyze` at `src/index.tsx:2787`) sit behind the existing global browser-session guard at `src/index.tsx:787`; unauthenticated calls return 401 `AUTH REQUIRED`.
  - Pinned allowlist: `PINNED_MODEL = 'gpt-5-mini-2025-08-07'` at `src/index.tsx:2404` is the only model bound in requests and accounting; `modelBaseURL` at `src/index.tsx:2444` accepts only an exact HTTPS `OPENAI_BASE_URL` that appears in `OPENAI_ALLOWED_BASE_URLS`, so a configured URL can never approve itself.
  - Bounds: per-request user input is capped at `MODEL_USER_INPUT_CHARS = 16000` (`src/index.tsx:332`); the assembled prompt including both system layers is capped at `MODEL_TOTAL_INPUT_CHARS = 48000` and checked at `src/index.tsx:2551` before provider contact. Output is bounded by `max_completion_tokens = 1300` and a 12,000-character validated answer ceiling; the request timeout is 15,000 ms with `MODEL_MAX_ATTEMPTS = 2` bounded retries via `AbortController`.
  - Rate and budgets: `reserveModelRequest` at `src/index.tsx:2502` performs one atomic INSERT whose SQLite `BEFORE INSERT` trigger enforces 10 requests/rolling minute, 100 requests and 20,000 reserved output tokens/UTC day, and 2,000 requests and 100,000 reserved output tokens/UTC month per owner. There is no read-then-write window. `modelLimitEvent` at `src/index.tsx:2494` translates the trigger abort into 429 with `Retry-After: 60` for rate limiting.
  - Prompt-injection layer order (`MASTERPROMPT.md` line 160) is exact: system policy (`HERMES_SYSTEM`), application policy (`MODEL_POLICY` at `src/index.tsx:2397`), user request, then `<UNTRUSTED_RETRIEVED_SOURCE_CONTENT>`, `<UNTRUSTED_PERSONAL_JOURNAL_CONTENT>`, and `<UNTRUSTED_QUOTED_EXTERNAL_MESSAGES>` fences built by `fencedModelData` at `src/index.tsx:2440`. Council rows authored outside the browser session carry `EXTERNAL_MESSAGE_PREFIX` (`src/index.tsx:2412`, applied at `src/index.tsx:3081`) and are split out of the journal fence into the least-trusted quoted-external layer at `src/index.tsx:2699-2725`.
  - Authorisation outside the model: no model output reaches D1 until `modelAnswerSchema` (`z.strictObject`, `src/index.tsx:2413`) validates it, and every persistence path is a server-side owner-filtered write. `POST /api/intel/:id/analyze` re-scopes its UPDATE by `id` and `user_id`.
  - Graceful degradation and redaction: a missing key or unallowlisted URL returns 503 `MODEL SERVICE OFFLINE` with no reservation and no persisted message; upstream failures return `MODEL SERVICE UNAVAILABLE`, `MODEL REQUEST TIMED OUT`, or `MODEL RESPONSE INVALID` and never echo raw provider text, configuration, or keys.
  - Audit evidence: `modelAudit` at `src/index.tsx:2459` appends metadata-only rows (owner, request id, route, pinned model, event type, attempt, character/token counts, upstream status). No prompt, answer, credential, key, or raw upstream error is stored. This is model-specific accounting; the general Book 5.7 `audit_events` table is separate and still open.
  - Evidence: `test/model-security.test.ts` (15 tests) and `test/migration-0007.test.ts` (2 tests). `migrations/0007_model_security.sql` is additive with an in-file rollback note.

### 5.7 Audit and idempotency

- B5.7 [DONE — SOURCE/LOCAL VERIFIED] `migrations/0008_audit_idempotency.sql` adds `audit_events` with the exact MASTERPROMPT column set, both mandated indexes, and append-only `BEFORE UPDATE`/`BEFORE DELETE` triggers plus an actor-type guard; and `idempotency_keys` with `UNIQUE(user_id, scope, request_id)` storing the first response for verbatim replay. `src/index.tsx` adds `requestId()` (validated `X-Request-Id`), metadata-only `auditEvent()`, and `withIdempotency()` (atomic `INSERT OR IGNORE` arbiter). Fifteen consequence-bearing routes are wrapped — reward redemption, intel, block log, unit step, tongue capture/review/exam, debrief, law check, library chapter, the four bridge writes, and the model routes. The two racy enforcement bonus guards (MVD, victory) are converted to atomic conditional INSERTs. `x-request-id` added to the CORS preflight allowlist and `Access-Control-Allow-Headers`. Frontend and `hermes_bridge.py` generate one id per intent and reuse it on retry. Evidence: `test/audit-idempotency.test.ts` (12) and `test/migration-0008.test.ts` (6).

### 5.8 Blocking acceptance matrix

- B5.8.1 [COVERED — EXHAUSTIVE] Unauthenticated private access denied — `test/route-denial.test.ts` derives the full private-route table from `src/index.tsx` source at runtime and asserts every browser route (51), every agent route (9), and the internal job all deny an anonymous or forged-credential caller with 401/403, that denial bodies leak no personal data, that query-string agent tokens are rejected, and (session lifecycle) that a valid session is accepted, a revoked session and an expired session are rejected, and neither raw sessions nor raw agent credentials are stored. Logout is documented open-by-design and pinned by its own test.
- B5.8.2 [COVERED] Valid session accepted — `test/session-ownership.test.ts:127-142` proves a real hashed session is accepted; calendar success is also asserted at `test/security-boundary.test.ts:103-106`.
- B5.8.3 [COVERED] Expired session rejected — `test/session-ownership.test.ts:144-164` covers fixed-past and production ISO timestamps.
- B5.8.4 [COVERED] Logout revokes the server-side session — `test/session-ownership.test.ts:167-181`.
- B5.8.5 [COVERED FOR CURRENT PERSONAL SURFACE] Cross-user reads, record updates, append attempts, state totals, calendar export, Hermes briefing/model context, agent export, predictions, responses, and load reductions are denied or filtered in `test/session-ownership.test.ts:215-525`.
- B5.8.6 [COVERED — FOCUSED LOCAL TESTED] Agent scopes are enforced per logical route at `src/index.tsx:730-828`; `test/agent-credentials.test.ts:187-236` proves a `blocks:read` credential can read pending blocks but cannot read briefing or write a block, and unversioned access is retired. `test/agent-credentials.test.ts:238-256` separately proves a query-string token is rejected when the required header is absent.
- B5.8.7 [COVERED — FOCUSED LOCAL TESTED] Revoked agent credentials are denied at `test/agent-credentials.test.ts:258-277`.
- B5.8.8 [COVERED — FOCUSED LOCAL TESTED] Expired agent credentials are denied at `test/agent-credentials.test.ts:279-290`.
- B5.8.9 [COVERED — FOCUSED LOCAL TESTED] Raw credentials are returned only by issuance POST, SHA-256 hashes are stored, GET listing omits token/hash, and migration `0006` erases the legacy plaintext value; evidence is `test/agent-credentials.test.ts:127-185` and `test/migration-0006.test.ts:17-55`.
- B5.8.10 [COVERED — FOCUSED LOCAL TESTED] Default credentials exclude `export:read`; export is denied until an explicitly scoped credential is issued at `test/agent-credentials.test.ts:293-314`. The bridge adds a separate `--authorize-full-export` requirement at `test/hermes-bridge.test.ts:23-27`.
- B5.8.11 [COVERED] `GET /api/state` performs zero writes — `test/get-read-only.test.ts:113`.
- B5.8.12 [COVERED] Repeated GET produces zero penalties — `test/get-read-only.test.ts:113` repeats GET/HEAD and instruments all mutating SQL.
- B5.8.13 [COVERED] Crawler-pattern access alters nothing — `test/get-read-only.test.ts:113` and all-route coverage at `test/get-read-only.test.ts:131`.
- B5.8.14 [COVERED — FOCUSED LOCAL TESTED] Model endpoint denies unauthenticated calls — `test/model-security.test.ts:92` asserts 401 `AUTH REQUIRED` on all three model routes with no session.
- B5.8.15 [COVERED — FOCUSED LOCAL TESTED] Model rate limit fires — `test/model-security.test.ts:534` allows ten requests per rolling minute, denies the eleventh with 429 `MODEL RATE LIMIT EXCEEDED` and `Retry-After: 60`, and proves the provider was called exactly ten times. `test/model-security.test.ts:562` proves daily and monthly request/token budgets deny before any provider contact, and `test/migration-0007.test.ts:53` proves the D1 trigger itself rejects the eleventh reservation.
- B5.8.16 [COVERED — FOCUSED LOCAL TESTED] Model input-size limit fires — `test/model-security.test.ts:149` rejects oversized retrieved context with 413 `MODEL INPUT TOO LARGE` and no provider call; `test/model-security.test.ts:199` proves the two system/policy layers are counted inside the 48,000-character total bound.
- B5.8.17 [COVERED — FOCUSED LOCAL TESTED] No key or raw upstream leakage — `test/model-security.test.ts:483` proves a 503 upstream body containing the test key is never echoed and retries stop at two attempts; `test/model-security.test.ts:505` proves a redacted timeout; `test/model-security.test.ts:234` and `test/model-security.test.ts:257` prove an unallowlisted or unallowlisted-by-omission base URL yields 503 without disclosing configuration; `test/model-security.test.ts:119` proves the offline path never names the key variable.
- B5.8.18 [COVERED — FOCUSED LOCAL TESTED] Retrieved content cannot authorize a write — `test/model-security.test.ts:320` stores an injection instructing the model to acknowledge a flag, proves it travels only inside `<UNTRUSTED_RETRIEVED_SOURCE_CONTENT>` alongside the "Tool authorisation lives outside the model" policy, and proves the seeded `honesty_flags` row remains `acknowledged = 0`. `test/model-security.test.ts:377` proves externally authored council rows are fenced in the separate least-trusted `<UNTRUSTED_QUOTED_EXTERNAL_MESSAGES>` layer, ordered after the personal-journal fence and never mixed into it.
- B5.8.19 [COVERED — FOCUSED LOCAL TESTED] Structured output is schema-validated — `test/model-security.test.ts:428` proves an extra `authorizeWrite` field is rejected with 502 `MODEL RESPONSE INVALID`, no message is persisted, and an `invalid_output` audit event is recorded; `test/model-security.test.ts:464` proves an oversized answer is rejected; `test/model-security.test.ts:624` proves audit rows are metadata-only and that UPDATE and DELETE both abort with `MODEL_AUDIT_APPEND_ONLY`.

## Book 6 — Phase 1: Correctness and Performance

- B6.1 [DONE] Canonical time. Every route derives the civil date from `settings.timezone` via `userNow()`/`safeDate()`; `POST /api/tongue` uses it. Client dates are accepted only for read-only history and clamped never-future. DST is explicitly tested — spring-forward, fall-back, half-hour offset, southern-hemisphere, and the unusable-zone fallback — in `test/timezone-dst.test.ts` (12).
- B6.2 [DONE] Read amplification. `computeStreak` and stats read the materialized `day_summary` (one indexed query, no N+1). Measured with a statement-counting binding in `test/query-budget.test.ts`: `/api/state` = 19 statements and stays **flat (19 → 19) when history doubles from 120 to 240 finalized days**; `/api/stats` = 20. Historical reads are bounded (≤200).
- B6.3 [DONE] Polling removed. New `GET /api/version` returns a weak ETag over per-table max markers plus the civil minute and answers 304 when nothing changed; the client refreshes on focus, visibilitychange, reconnect, after any successful mutation, and at the next block boundary — never on a fixed interval. The alarm ticker's refresh routes through the same conditional probe. Evidence: `test/freshness.test.ts` (12).
- B6.4 [DONE] Rendering. Vendored `public/static/morph.js` morphs the tab in place, preserving focus, caret/selection, half-typed textarea text, and keyed-node identity; textarea/input drafts also persist to localStorage. Proven against a real DOM in `test/morph.dom.test.ts` (10, happy-dom).
- B6.5 [DONE] Data integrity. `nl2br` and `mdLite` escape before adding only whitelisted formatting — model output cannot inject markup, proven against attack payloads in `test/xss-escaping.dom.test.ts` (4). Card review and tongue capture multi-writes are wrapped in `DB.batch()`; the two enforcement engines were already collapsed into one `runEnforcement` pass invoked only by `POST /api/tick` and the internal cron job (never by a GET).
- B6.6 [MEASURED] Bundle: `dist/_worker.js` ≈ 195 kB; frontend static JS+CSS ≈ 154 kB (excl. 2.7 MB book JSON, whose lazy-split is a Book 7 item). Query counts per route as above. Model latency is bounded server-side by the Book 5.6 timeout/retry caps.

## Book 8.2 / 8.6 — Phase 2: /catchup and Recovery Scoring

- B8.6 [DONE — SOURCE/LOCAL VERIFIED] `POST /api/catchup` emits the six-part re-entry protocol in the fixed order — what was missed (from the record, never rewritten), likely mechanism (from the 8.4 taxonomy), what not to do (never backlog, make-up load, or self-critical logging), the minimum viable recovery, one structural patch (time/environment/cue/scope/support), and tomorrow's single protected keystone with a start time, environment, and first physical action. Absence over fourteen days adds the five-question diagnostic and re-seats the ratchet at its base. Deterministic and offline-safe (no model call). Each run is recorded in append-only `catchup_sessions`. `GET /api/state` carries a cheap one-query `needsCatchup` signal (three dark days) and the NOW tab shows the re-entry door.
- B8.2 [DONE — SOURCE/LOCAL VERIFIED] Minimum Viable Recovery: `POST /api/recovery` logs the single restoring action (one per day, idempotent) into `recovery_actions` and re-materializes the day so `day_summary.mvr_held = 1`. `computeStreak` and `writeDaySummary` treat `mvr_held` as **survival, never victory** — the streak lives, it does not advance. Migration `0009_recovery_catchup.sql` is additive with a rollback note. Evidence: `test/recovery-catchup.test.ts` (16) and `test/catchup-wiring.test.ts` (5).

## Book 17 — Fixed Execution and Reporting

- B17.1 [IN PROGRESS] Fixed sequence: finish Book 5/5.8; Book 6; `/catchup` plus Books 8.6 and 8.2 recovery scoring; Book 13.2 alternative-explanation gate; Books 14 and 16 Continuity Brief/chapter cursor; Book 7 refactor; Book 9 UX/ratchet; Book 10 learning; Books 11–12 rhetoric; Book 13 decision lab; Book 15 Hermes offices; PWA/accessibility/performance.
- B17.2 [DONE] Baseline rollback base is `a927e97`; first-slice commits are `c3e3557`, `f725397`, `1c25ce9`, and `ad19313`.
- B17.3 [DONE] First-slice verification: 9 tests across 3 files, Vite build, TypeScript no-emit check, Cron Worker Wrangler dry run, and `git diff --check` passed on 2026-08-15.
- B17.4 [DONE] Account/operator documentation is consolidated into root `OPERATIONS.md`; `OPERATIONS_FIRST_SLICE.md` is superseded and removed.
- B17.5 [IN PROGRESS] Every schema change must add a numbered migration, migration-specific rollback note in `OPERATIONS.md`, and a test against a local D1/schema copy before code may be reported complete. Book 5.2 satisfies this repository gate through `migrations/0005_sessions_and_ownership.sql`, `OPERATIONS.md` Section 5.1, and `test/migration-0005.test.ts`. Book 5.5 satisfies it through `migrations/0006_agent_credentials.sql`, `OPERATIONS.md` Section 5.2, and `test/migration-0006.test.ts`. Book 5.6 satisfies it through `migrations/0007_model_security.sql`, `OPERATIONS.md` Section 5.3, and `test/migration-0007.test.ts`. Production application remains operator-controlled.
- B17.6 [IN PROGRESS] Commits remain separated by concern. Push `refactor/strategic-judgment-os` at each completed phase boundary. Never claim production application or deployment.
- B17.7 [DONE — COMMITTED/PUSHED] Book 5.2 source, migration, regression coverage, and documentation are committed in the branch history; production migration and deployment remain unclaimed and operator-controlled.
- B17.8 [DONE — COMMITTED/PUSHED] Book 5.4 headers, CORS, no-store, CSRF, payload limits, strict request validation, mass-assignment prevention, and legal-transition controls passed the evidence recorded in B5.4.5 and are committed in the branch history. This slice has no schema migration. No production deployment or operator action is claimed.
- B17.9 [DONE — COMMITTED/PUSHED] Book 5.5 source, migration, tests, frontend credential interface, and safe bridge passed the evidence recorded in B5.5 and are committed as `6b37725` with documentation in `b8af2d9`; both are pushed to `refactor/strategic-judgment-os`. Independent specification and quality reviewers both failed with provider HTTP 429 before returning findings, so no reviewer approval is claimed. Phase 0 remains IN PROGRESS because Book 5.7 and the partial Book 5.8.1 acceptance item remain open.
- B17.10 [DONE — SOURCE/LOCAL VERIFIED] Book 5.6 model security, prompt-injection defense, and cost control passed the evidence recorded in B5.6 and B5.8.14–B5.8.19. Final local verification on 2026-08-19: `test/model-security.test.ts` and `test/migration-0007.test.ts` passed 17/17; the complete suite passed 87/87 across 12 files; `npx tsc --noEmit` passed; the Vite production build transformed 108 modules and emitted `dist/_worker.js` at 186.26 kB; `git diff --check` reported no whitespace errors and only LF-to-CRLF working-copy warnings. A value-suppressing scan of 58 source, public-asset, worker, migration, test, and root-configuration files found zero private-key, GitHub-token, OpenAI-key, JWT, or bearer-credential signatures, and no tracked file matches a secret-bearing name. Three genuine defects were found by test-first work and fixed rather than documented around: a base-URL allowlist that let a configured URL approve itself, use of the Responses-API field `max_output_tokens` instead of the Chat Completions field `max_completion_tokens`, and a total-input bound that excluded the system and application policy layers. `vitest.config.ts` now pins the test compatibility date to the `wrangler.jsonc` value so the pool no longer defaults to the current calendar date and fails against the installed `workerd` binary. Independent reviewer dispatch was not attempted in this slice because the operating instructions for this session prohibit subagent dispatch unless requested; no reviewer approval is claimed. No production migration, deployment, secret configuration, credential issuance/rotation, or other operator action is claimed.
